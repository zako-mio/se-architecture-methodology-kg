# 插件 DAG 两次级联升级 (Plugin DAG Knowledge Base RC7 to RC8 to rc2 Cascade Upgrade)

- ID: `CAS-X-07`
- 类型: 案例
- 所属组: GC-CASE 案例层
- 阶段: 高级 advanced
- 域: X
- 横切主题: P
- 生命周期: active

## §1 摘要与结论

> 在同一知识库上继续两轮上游级联升级：处理数百次提交与包的增删，暴露「依赖声明不等于运行时引用」、分层须按最长路径深度、纯外部依赖边不得入主图等差分陷阱，并以全量门控校验收口。

- 别名：依赖声明不等于运行时引用、最长路径深度分层、外部 seam 边不入主图
- 英文：Plugin DAG Knowledge Base RC7 to RC8 to rc2 Cascade Upgrade
- 类型：案例 / maintenance

## §2 局部关系图

以本节点为中心的 1–2 跳邻域（构建期静态派生；HTML 版为内联 SVG）。

| 跳数 | 节点 ID | 名称 |
|---|---|---|
| 0 | `CAS-X-07` | 插件 DAG 两次级联升级 |
| 1 | `ESS-H-04` | 抽象泄漏 |
| 1 | `MTH-L-05` | 架构适应度函数 |
| 1 | `MTH-O-10` | 增量演化与一致性治理 |
| 2 | `CAS-X-06` | 插件 DAG RC5→RC7 升级 |
| 2 | `CAS-X-14` | OpenSpec 规约驱动解析 |
| 2 | `ESS-H-01` | 信息隐藏 |
| 2 | `ESS-H-09` | 设计模式与反模式 |
| 2 | `ESS-L-03` | 架构侵蚀与漂移 |
| 2 | `ESS-O-03` | 权衡的不可消除性 |
| 2 | `MTH-L-04` | 演进式架构 |
| 2 | `MTH-O-11` | 度量与反馈闭环 |
| 2 | `TEC-L-01` | 架构适应度函数工具化 |

邻域内关系边：

| 类型 | 方向 | 说明 |
|---|---|---|
| 案例印证 | CAS-X-06 → MTH-L-05 | 印证：以可执行门控持续评估知识库架构完整性 |
| 案例印证 | CAS-X-06 → MTH-O-10 | 印证：以增量同步与契约替代全量重建的一致性治理 |
| 案例印证 | CAS-X-06 → TEC-L-01 | 印证：把架构约束转写为自动执行、可量化判定的校验 |
| 案例印证 | CAS-X-07 → ESS-H-04 | 印证：依赖声明是泄漏的抽象，不能替代运行时真实引用 |
| 案例印证 | CAS-X-07 → MTH-L-05 | 印证：用可执行门控持续校验图谱架构完整性 |
| 案例印证 | CAS-X-07 → MTH-O-10 | 印证：以增量同步与契约替换全量重建 |
| 案例印证 | CAS-X-14 → MTH-O-10 | 印证：Delta Spec 增量叠加与归档合并 |
| 对照 | ESS-H-04 → ESS-H-01 | 抽象泄漏揭示信息隐藏的固有边界 |
| 对照 | ESS-H-09 → ESS-H-04 | 模式封装复杂度，抽象泄漏标明其边界 |
| 依赖关联 | MTH-O-10 → MTH-O-11 | 增量演化治理依赖度量反馈闭环 |
| 依据推导 | MTH-L-05 → ESS-L-03 | 适应度函数用于抑制架构侵蚀与漂移 |
| 依据推导 | MTH-O-10 → ESS-O-03 | 演化中的一致性取舍依据权衡不可消除 |
| 依据推导 | MTH-O-11 → ESS-O-03 | 度量与反馈是对取舍效果的检验 |
| 实现 | TEC-L-01 → MTH-L-04 | 工具化持续校验支撑演进式架构的增量变更原则 |
| 实现 | TEC-L-01 → MTH-L-05 | 适应度函数工具化实现架构适应度函数方法论 |
| 细化 | MTH-L-05 → MTH-L-04 | 适应度函数是演进式架构的强制机制 |

## §3 原理 · 为什么

这一案例把「版本升级方法论」从一次性经验推进为可复用的判定法则，其价值在于三个反直觉发现。第一，依赖声明层的大规模解耦不等于运行时依赖消失：大量界面包移除了对公共基础包的声明，但源码仍在导入它们，若只看清单增删会误删大量真实依赖边；应以源码实际引用交叉验证。第二，分层不能用广度优先层次：那会把所有无依赖节点堆积在零层，语义失真，正确做法是按最长路径深度计算，被依赖方先于依赖方，零层是基础能力。第三，指向外部基座的依赖边不能混入主图边集，否则渲染脚本会因端点缺失而崩溃；这类依赖应经外部基座表按其被依赖集合表达。三条都是「数据表示必须匹配语义」的具体体现，共同指向一个更一般的判据：当图谱的语义（运行时依赖、拓扑深度、外部边界）与它的载体（依赖声明、遍历层次、单一图集）不一致时，必须改载体而非迁就载体。换言之，差分维护的可信度取决于载体能否忠实承载语义，一旦发现偏差，就应修正数据模型而非修补表象，否则同一误差会在每次升级中重复出现。

## §4 机制 · 如何运作

第二轮升级的真实规模为数百次提交、包数由两百出头增至两百二十余、新增若干删除若干、源码变更包数十个，节点由图谱相应增删、边与组数同步增长。处理要点包括：对每条「声明被删除的依赖」用脚本检索源码是否仍引用，仍引用则保留边并在相应节点页注入「声明调整」标注，确认真删才删边；重算分层后必须重建「层号到节点列表」的映射，否则门控会报层未覆盖全部节点；把纯外部基座依赖从主图边中移出，改由外部基座表的被依赖字段与引用计数表达，双身份节点可例外入边；对结构不兼容的持久化格式变更（模式版本、打包与压缩方式）单独标注需重建数据库；对改名与合并的包同步更新节点标识。新增判定还体现「声明与实现的分离」：包清单是发布者的声明，源码导入是运行时的实现，二者可合法地不一致；图谱要表达运行时真相，因此必须以源码为准并保留声明差异的标注，让读者既看到真实耦合、也看到发布者的意图调整。

## §5 工程 · 怎么落地

工程上仍复用既有生成与门控脚本全量重生成，并针对新增语义补充检查：声明删除边的源码引用交叉验证脚本、分层重算与映射重建、外部基座边与主图边的分离、节点标识与页面文件名的后缀一致性防动态跳转死链。验证沿用分层工具链：先静态断链检查，再做标识与页面存在性映射，再对模板动态跳转代入全部标识，最后以无头渲染加视觉复核确认交互。分层工具链验证的顺序设计值得复用：先做零成本的静态断链检查，再做标识与页面存在性映射，再对模板动态跳转代入全部标识，最后才用较重的无头渲染与视觉复核；把昂贵验证放最后，可让前序步骤尽早拦截大部分问题，显著降低整体验证成本。升级后的统计口径（节点、边、层、组的增减）写入附带版本差异报告，作为可追溯的演进记录，而非覆盖式更新。

## §6 权衡卡

### 代价 · 风险

- 「按源码实际引用保留边」提高了图谱保真度，却使依赖边不再与清单声明一一对应，读者必须理解「声明调整」标注的含义，数据维护者也要承担交叉验证成本；换来的是不会因上游清理声明而丢失真实耦合。
- 把外部基座边移出主图消除了渲染崩溃，代价是完整依赖视图被拆成主图与外部表两部分，读者需同时查阅。
- 特判规则会随每次升级累积，形成方法自身的债务：规则越多越应与图谱同源维护，否则规则滞后会让下一轮升级误判——这是所有差分维护系统的共性代价，缓解之道是把规则也当成受门控的产物。

### 原文要点

- 最长路径深度分层语义更准，但对存在多路径的图更敏感，一处依赖增删可能级联改变多层编号，需要完整重算而非局部修补。
- 区分改名、合并与结构不兼容变更保证了正确性，却使每轮升级都要维护更多特判规则；这些规则本身若不随上游演进及时更新，会成为下一轮升级的负债。

> 权衡原文仅按显式标记词归栏引用（收益 / 代价 / 反例），其余句子按原序列为「原文要点」；内容逐字引用未改写。

## §7 案例带

- **CAS-X-04**（2 跳派生：当前节点 → MTH-O-10 → ESS-O-03）网申 v2 深度重构：一次由「改一处漏多处」症状触发的深度重构：定位到接口定义散落 12 个文件、存在 7 种接口形态的根因后，以单一契约源、dispatch 消息表与能力注册中心重建三层契约系统，恢复可审计与可校验性。
- **CAS-X-06**（经 MTH-O-10）插件 DAG RC5→RC7 升级：一次知识库随上游源码版本升级的级联维护：以三层差异分析把 219 个包收敛为 26 个实质变更包，经编译、运行时、组合三类依赖判定更新 DAG 数据后全量重生成页面，并以静态门控加渲染验证收口。
- **CAS-X-14**（经 MTH-O-10）OpenSpec 规约驱动解析：对比规约驱动开发工具链 OpenSpec 与 Superpower 的元模型、工件依赖 DAG 与安全门控，提炼「规约先于实现」工程形态的解析案例。
- **CAS-X-15**（2 跳派生：当前节点 → MTH-L-05 → TEC-L-01）Open Code Review 图谱：对阿里开源 AI 代码审查 CLI 做源码级解析并构建「知识 + 实践资产 + AI 索引」三合一知识图谱的工程案例。
- **CAS-X-16**（2 跳派生：当前节点 → MTH-O-10 → MTH-O-11）控制理论⊗Agent 设计：将控制理论五维（负反馈 / 状态估计 / PID / 鲁棒 / 分层）系统映射到 Agent 工程环节，并以真实产品案例验证的跨学科方法论案例。
- **CAS-X-17**（2 跳派生：当前节点 → MTH-O-10 → MTH-O-11）技术知识中枢交付法：从模糊需求到上线，把 AI Agent 驱动的端到端交付固化为四阶段加双共识加迭代的可迁移方法论的实战提炼案例。
- **CAS-X-18**（2 跳派生：当前节点 → MTH-L-05 → TEC-L-01）AI Agent 架构 DAG 复盘：对四个工作日内独立完成三个异构对象 DAG 分析的方法论复盘，总结「采集→建模→生成→门控→交付」可复用管线与关键教训。

## §8 信源与核验

- 核验状态：True（verified）
- 置信度：high
- 复核日期：2026-09-10
- 生命周期：active
- 规范信源：LOC-026、BK-011、OFF-022
- 勘误：常见误解「包清单里删掉了依赖声明，就应同步删掉图谱中对应的依赖边」→ 核验结论「源码仍导入时运行时依赖并未消失；应按源码实际引用交叉验证，仍引用则保留边并标注「声明调整」，仅确认真删时才删边」（LOC-026）

## §9 原始关系清单

| 关系 | 方向 | 对端节点 | 边 ID | 说明 |
|---|---|---|---|---|
| 案例印证 | CAS-X-07 → ESS-H-04 | 抽象泄漏（`ESS-H-04`） | E-958 | 印证：依赖声明是泄漏的抽象，不能替代运行时真实引用 |
| 案例印证 | CAS-X-07 → MTH-L-05 | 架构适应度函数（`MTH-L-05`） | E-957 | 印证：用可执行门控持续校验图谱架构完整性 |
| 案例印证 | CAS-X-07 → MTH-O-10 | 增量演化与一致性治理（`MTH-O-10`） | E-956 | 印证：以增量同步与契约替换全量重建 |

---

本页由 16-checkpoint/gen-md.py 生成；与 11-node-pages/CAS-X-07.html 同源同序。
